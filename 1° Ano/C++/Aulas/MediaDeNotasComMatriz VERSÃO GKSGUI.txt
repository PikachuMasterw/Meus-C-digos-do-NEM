#include <iostream>
using namespace std;

int main() {
    float notas[5][5];
    int pos = 0, q, n = 1, posição1 = 0;
    cout<<"escreva o maximo de X: ";
    cin>>q;
   q = q + 1;
    while(pos != q - 1){
    for(int posição = 0; posição < q - 1; posição++){
        cout<<"";
        cout<<"\nDigite a sua nota ("<<posição<<"/"<<pos<<")["<<n<<"/"<<(q - 1) * (q - 1)<<"]: ";
        cin>>notas[pos][posição];
         n = n + 1;
    }
   
    pos = pos + 1;
    }
    pos = 0;
   
    while(pos != q - 1){
        posição1 = 0;
        while(posição1 != q){
   notas[pos][q] =  (notas[pos][q]+ notas[pos][posição1]);
    posição1 = posição1 + 1;
      }
 cout<<"-nota("<<pos<<"/"<<q - 1<<") = "<<notas[pos][q] / (q - 1)<<"\n";
    pos = pos + 1;
    }
    
    
    
    
    
    return 0;
}