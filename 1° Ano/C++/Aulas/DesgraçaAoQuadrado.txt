#include <iostream>
using namespace std;
#include <math.h>

int main() {
    int b = 2, n = 1;
    
    cout<<"Digite a base da potência: ";
    cin>>b;
    
    cout<<"Digite o expoente da potência: ";
    cin>>n;
    if((b >= 2) or (n > 1)){
    cout<<"Resposta: "<<pow(b,n);
    }
    else{
        main();
    }

    
    return 0;
}